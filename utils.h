#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* removeID(char* ) ;

char* getString(char* , int*) ;

int getNumero(char *, int *) ;

int incrementar(char **, int ) ;

int agregar(char**, int , char*) ;
